using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AI2 : MonoBehaviour
{
    enum agentState;
    enum selfState { };

    enum gameState;

    while(gameState == not_over){

        switch(agentState){
        case(playerClose){//stab
        }
        case(playerFar){//run to player}
        }


    }
}
