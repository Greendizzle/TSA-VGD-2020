//All the using stuff...


class Ai{
    enum agentState;
    enum selfState;

    enum gameState;

    while(gameState == not_over){

        switch(agentState){
        case(playerClose){//stab
        }
        case(playerFar){//run to player}
        }


    }

}
